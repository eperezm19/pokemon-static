import { Navbar } from '@common/index';
import { StyledHeader } from '@styles/Header.styled';
import React from 'react';

export const Header = () => {
  return (
    <StyledHeader>
      <Navbar />
    </StyledHeader>
  );
};
